---
title: "Auth"
description: "The 4 octolens commands in the auth group: login, logout, switch, whoami."
---

{/* GENERATED by `pnpm cli:docs:site` from docs/cli/contract.json (manifest version 1) in the octolens/octolens repo. Do not edit by hand: regenerate there and re-sync. */}

Flags and arguments below are the contract as the binary states it. The global flags (`--json`, `--timeout`, …) apply here too and are documented on [Global flags](/cli/contract/global-flags).

## `octolens login`

Authenticate the CLI (browser handoff or API key)

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `KEY` | `string` | optional | API key to save (with --with-key); prefer the masked prompt |

**Flags**

| Flag | Type | Required (headless) | Description |
|---|---|---|---|
| `--no-browser` | `boolean` | no | Print the authorize URL instead of opening a browser, then wait for the key to arrive out of band. The wait is time-bounded: the global --timeout governs it (default 180s without the flag), and expiry exits 2 with a LOGIN_TIMEOUT envelope — the process always terminates on its own |
| `--profile` | `string` | no | Name to store this login under |
| `--scope` | enum: `read`, `write`, `admin` | no | Scope to mint during the browser handoff (default: `write`) |
| `--with-key` | `boolean` | no | Authenticate with an API key (headless boxes) instead of the browser — create one at https://app.octolens.com/me/api |

**Examples**

```bash
octolens login
octolens login --with-key
octolens login --with-key ak_live_xxx
```

**Exit codes** — `0` OK · `1` UNEXPECTED · `2` USAGE · `8` CANCELLED. The full map is on [Exit codes](/cli/contract/exit-codes).

**Error codes** — `BROWSER_LOGIN_REQUIRES_TTY`, `LOGIN_TIMEOUT`, `MISSING_KEY_VALUE`. Every code is listed on [The error envelope](/cli/contract/error-envelope).

## `octolens logout`

Remove stored login credentials

**Flags**

| Flag | Type | Required (headless) | Description |
|---|---|---|---|
| `--all` | `boolean` | no | Remove every stored profile |
| `--profile` | `string` | no | Name of the profile to remove |

**Examples**

```bash
octolens logout
octolens logout --profile acme
octolens logout --all
```

**Exit codes** — `0` OK · `1` UNEXPECTED · `2` USAGE. The full map is on [Exit codes](/cli/contract/exit-codes).

**Error codes** — `NO_STORED_PROFILE`. Every code is listed on [The error envelope](/cli/contract/error-envelope).

## `octolens switch`

Switch the active workspace profile

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `PROFILE` | `string` | optional | Profile name (or 1-based number) to switch to |

**Examples**

```bash
octolens switch acme
octolens switch --json
```

**Exit codes** — `0` OK · `1` UNEXPECTED · `2` USAGE · `3` AUTH · `4` NOT_FOUND · `8` CANCELLED. The full map is on [Exit codes](/cli/contract/exit-codes).

**Error codes** — `INVALID_PROFILE_CHOICE`, `MISSING_PROFILE`, `NO_PROFILES`, `NO_PROFILE_SELECTED`, `PROFILE_NOT_FOUND`. Every code is listed on [The error envelope](/cli/contract/error-envelope).

## `octolens whoami`

Show the active login: workspace, edition, auth source (env vs profile), and scope

**Examples**

```bash
octolens whoami
octolens whoami --json
```

**Exit codes** — `0` OK · `1` UNEXPECTED · `2` USAGE · `3` AUTH. The full map is on [Exit codes](/cli/contract/exit-codes).

**Error codes** — `NOT_LOGGED_IN`. Every code is listed on [The error envelope](/cli/contract/error-envelope).
